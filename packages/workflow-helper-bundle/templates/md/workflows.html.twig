Markdown for {{ workflowName }}

{% for type, e in events %}

{% for code, x in e %}

## {{ type }}.{{ code }}

{% for a in x %}
```php
{{ a.source|raw }}
```
{{ a.link }}
        {% endfor %}

    {% endfor %}
{#        {% for x, event in e %}#}
{#{{ e.link }}#}
{#{% endfor %}#}

{%- endfor -%}
