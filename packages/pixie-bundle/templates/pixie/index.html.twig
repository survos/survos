{% extends '@SurvosPixie/pixie/layout.html.twig' %}

{% block title %}Pixies{% endblock %}

{% block body %}

    {#    <p class="mb-1 text-muted">{{ _self }}</p>#}
    <h3 class="mb-3">{{ 'Pixies' }}</h3>

    <h3>
        Search Pixies
        <span class="htmx-indicator">
    <img src="/img/bars.svg"/> Searching...
   </span>
    </h3>

<form
                hx-get="{{ path('pixie_config_search') }}"
      hx-target="#search-results"
                hx-trigger="search, submit, load, input changed delay:400ms from:find input[type='search'], input delay:100ms from:find input[type='checkbox']">

    <input class="form-control" type="search" name="q" placeholder="Begin Typing To Search Users..." />
    <input name="active_only" type="checkbox" />show counts
</form>

<div id="search-results">

</div>

{#<form#}
{#        hx-get="{{ path('pixie_config_search') }}"#}
{#        hx-target="#search-results"#}
{#        hx-trigger="load"#}
{#        hx-indicator=".htmx-indicator"#}
{#    >#}

{#    <input class="form-control"#}
{#           hx-get="{{ path('pixie_config_search') }}"#}
{#           hx-target="#search-results"#}
{#           hx-trigger="load, input changed delay:400ms, search"#}
{#           type="search"#}
{#           name="q" placeholder="Begin Typing To Search Users..." />#}
{#    <input name="active_only"#}
{#           hx-trigger="input changed delay:100ms, search"#}
{#           type="checkbox"/>Active Only#}
{#</form>#}

{#    <div id="search-results">#}
{#        x#}
{#    </div>#}

    <div>

{% endblock %}
